const assert = require('assert');
const chordIllustrator = require('../index.js');

describe('chordIllustrator', () => {
  it('has a test', () => {
    assert(false, 'chordIllustrator should have a test');
  });
});
